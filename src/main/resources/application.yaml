spring:
  application:
    name: SpringBootShop_non_JWT
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
    open-in-view: false

  profiles:
    active: uat

#dev:
#  nguyen: Nguyen-Van-Quyet

server:
  port: 8081